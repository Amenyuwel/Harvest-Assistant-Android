<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="16dp">

    <EditText
        android:id="@+id/et_crop_damage_severity_1"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Severity Level 1"
        android:inputType="text" />

    <EditText
        android:id="@+id/et_crop_damage_severity_2"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Severity Level 2"
        android:inputType="text" />

    <!-- TextView to display the selected severity levels -->
    <TextView
        android:id="@+id/tv_selected_severity"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:text="Selected Severity:"
        android:textSize="16sp"
        android:textColor="@android:color/black" />
</LinearLayout>
